# Server Configuration
PORT=3001
NODE_ENV=development

# Google Gemini API Key
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Database Configuration
# For production, consider using PostgreSQL or another database
# For SQLite, use an absolute path for better compatibility
DATABASE_PATH=./chatbot.db

# Frontend URL (for CORS)
# In production, set this to your frontend domain(s)
# Multiple URLs can be comma-separated: https://example.com,https://www.example.com
FRONTEND_URL=http://localhost:5173

# API Key for Data Endpoints (optional but recommended)
# If not set, data endpoints will be publicly accessible
# Generate a secure random string for production
API_KEY=your_secure_api_key_here
